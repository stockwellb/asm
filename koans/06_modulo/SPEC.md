# Koan 06: Modulo

**Goal**: Exit with `17 mod 5` (i.e., 2).

**Practice**: reading the remainder from `rdx` after `div`.

**Success**: `echo $?` prints `2`.

---

**Hints**:
- same `div` setup as koan 05
- the remainder lives in `rdx` after the instruction
- move `rdx` into `rdi` for the exit
